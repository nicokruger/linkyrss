{
  "name": "rss-atom-feed-processor",
  "version": "1.0.0",
  "description": "A Node.js application that takes the URL of an RSS/Atom feed and stores each article into Redis, then creates a new Atom feed from the articles.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "feed": "^4.2.2",
    "feedparser": "^2.2.10",
    "feedparser-promised": "^2.0.1",
    "redis": "^4.1.2"
  }
}
